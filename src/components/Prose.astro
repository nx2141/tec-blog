---
import { twMerge } from "tailwind-merge";
const { class: className = "" } = Astro.props;
---

<div
  class={twMerge(
    "prose text-white dark:prose-invert max-w-none prose-h2:flex prose-h2:font-semibold prose-h2:flex-col prose-h2:mb-8 prose-h2:justify-center prose-h2:text-3xl prose-h2:border-l-5 prose-h2:border-gray-600 prose-h2:py-2 prose-h2:pl-6",
    className
  )}
>
  <slot />
</div>
